/** 
 * @type {import('next').NextConfig}
 */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['picsum.photos'],
  },
  // output: 'export',
  // distDir: 'dist',

}

module.exports = nextConfig
