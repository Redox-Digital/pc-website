<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <title>Prise de contact - {{name}}</title>
    <style>
        /* Styles for email content */
        body {
            font-family: Tahoma, Arial,
                sans-serif;
            font-size: 16px;
            line-height: 1.5;
            color: #1d1f22;
        }

        .wrapper {
            max-width: 600px;
            margin: 0 auto;
            padding: 20px;
            background-color:
                #f9f7f1;
        }

        p {
            margin-top: 0;
            margin-bottom: 20px;
        }

        footer {max-width: 600px;
            margin: 0 auto;
            display:flex;
            flex-direction: row;
            justify-content: space-between;
            background-color: #000;
            color: #696969;
            padding: 10px 20px;
        }

        header {            max-width: 600px;
            margin: 0 auto;
            border-top: solid 3px #a88d44;
            background-color: #f9f7f1;
            padding: 20px;
            display: flex;
            flex-direction: column; 
        }

                header h1 {
            font-size: 24px;
            margin: 0;
            text-align: center;

        }

        header a {
            width: 100%;
            margin: 0 auto;
            text-align:
                center;
                color: #a88d44;
                font-size: 14px;
                text-decoration: none;
                letter-spacing: .03em;
        }
    </style>
</head>

<body>
    <header>
        <h1>Prise de contact</h1>
        <a href="https://pc-sa.ch" target="_blank" rel="noreferrer noopener">pc-sa.ch</a>
    </header>
    <div class="wrapper">


        <hr />
        <p>
          Nom: {{name}}
          <br/>Type de client: {{type}}
          {% if company != '' %}
          <br/>Entreprise: {{company}}
          {% endif %}
        </p>

        <p style="margin-bottom: 0px;"><small><b>Message</b>:</small></p>
        <hr>
        {{message}}
        <hr>

        <p>Coordonnées: <br/><a href="mailto:{{mail}}">{{mail}}</a><br/><a href="tel:{{phone}}">{{phone}}</a><br/></p>
    </div>
    <footer>
        <small>Paris &amp; Comtesse SA</small>
        <small>Baconnière 55, CH-2017 Boudry</small>
    </footer>
</body>

</html>